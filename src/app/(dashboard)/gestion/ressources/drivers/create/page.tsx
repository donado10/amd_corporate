import CreateDriverSection from "@/features/drivers/components/CreateDriverSection";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <CreateDriverSection />;
};

export default page;
